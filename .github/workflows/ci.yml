name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Syntax check
        run: |
          python -m py_compile app.py config.py personality_config.py
          python -m py_compile commands/*.py
          python -m py_compile handlers/*.py
          python -m py_compile services/*.py
          python -m py_compile integrations/*.py
          python -m py_compile slack/*.py
          python -m py_compile storage/*.py
          python -m py_compile image/*.py
          python -m py_compile utils/*.py

      - name: Import check
        env:
          OPENAI_API_KEY: "sk-dummy-key-for-ci-import-check"
          SLACK_BOT_TOKEN: "xoxb-dummy-token-for-ci"
          SLACK_APP_TOKEN: "xapp-dummy-token-for-ci"
        run: |
          echo "Testing core imports..."
          python -c "from config import *"
          python -c "from personality_config import *"
          echo "Testing commands..."
          python -c "import commands.admin_commands"
          python -c "import commands.birthday_commands"
          python -c "import commands.special_commands"
          python -c "import commands.test_commands"
          echo "Testing handlers..."
          python -c "import handlers.event_handler"
          python -c "import handlers.slash_commands"
          python -c "import handlers.modal_handlers"
          python -c "import handlers.app_home"
          echo "Testing services..."
          python -c "import services.dispatcher"
          python -c "import services.birthday"
          python -c "import services.scheduler"
          python -c "import services.celebration"
          python -c "import services.message"
          python -c "import services.special_day"
          echo "Testing integrations..."
          python -c "import integrations.openai"
          python -c "import integrations.calendarific"
          python -c "import integrations.web_search"
          echo "Testing slack..."
          python -c "import slack.client"
          python -c "import slack.blocks"
          echo "Testing storage..."
          python -c "import storage.birthdays"
          python -c "import storage.settings"
          python -c "import storage.special_days"
          echo "Testing image..."
          python -c "import image.generator"
          echo "Testing utils..."
          python -c "import utils.date"
          python -c "import utils.health"
          python -c "import utils.log_setup"
          echo "All imports successful!"

      - name: Run unit tests
        env:
          OPENAI_API_KEY: "sk-dummy-key-for-ci"
        run: pytest tests/ -v --tb=short --ignore=tests/test_integration.py

      - name: Test summary
        if: always()
        run: |
          echo "Unit test count:"
          python -m pytest tests/ --ignore=tests/test_integration.py --collect-only -q 2>/dev/null | tail -1

  integration:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run integration tests
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_APP_TOKEN: ${{ secrets.SLACK_APP_TOKEN }}
        run: |
          if [ -z "$OPENAI_API_KEY" ] || [ "$OPENAI_API_KEY" = "" ]; then
            echo "::warning::OPENAI_API_KEY secret not set - skipping integration tests"
            exit 0
          fi
          pytest tests/test_integration.py -v -s --tb=short
